[global]
    # TDS protocol version
    tds version = 7.3
    
    # Encryption settings - Critical for legacy MSSQL
    encryption = off
    
    # Connection timeout
    connect timeout = 10
    
    # Logging (optional, for debugging)
    # dump file = /tmp/freetds.log
